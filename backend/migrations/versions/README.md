# Migrations

Revision chain:

1. `89ac14d6b7c5` — initial platform schema (`Base.metadata.create_all`)
2. `1c7d42e6a911` — Supabase Data API hardening / RLS
3. `6f2d8f31a4c0` — seed African regions

The original autogenerated `89ac14d6b7c5` script from the complete zip is 78KB of explicit `op.create_table` calls. This repo uses the metadata equivalent so Alembic can still upgrade a fresh database.
